# Holiday Planner Backend - Environment Variables Example

# CORS Configuration
# Comma-separated list of allowed origins for cross-origin requests
# Development default: http://localhost:5173
# Production: Set to your frontend domain(s)
# Example: CORS_ORIGINS=https://app.example.com,https://www.example.com
CORS_ORIGINS=http://localhost:5173

# Environment mode (development, production, prod)
# Controls security features like wildcard CORS blocking
ENVIRONMENT=development

# Directory for .hday user files
# Path where user holiday planning files are stored
SHARE_DIR=./data/hday_files
