name: Android Staging APK

on:
  workflow_dispatch:
    inputs:
      api_base_url:
        description: "Staging API base URL (defaults to the build's placeholder when empty)"
        required: false
        type: string
      oidc_authority:
        description: "Staging OIDC authority (defaults to the build's placeholder when empty)"
        required: false
        type: string

permissions:
  contents: read

jobs:
  staging-apk:
    name: Assemble Staging APK
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Java
        uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5.4.0
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@3f131e8634966bd73d06cc69884922b02e6faf92 # v6.2.0

      - name: Assemble staging APK
        env:
          WORKTIME_ANDROID_STAGING_API_BASE_URL: ${{ inputs.api_base_url }}
          WORKTIME_ANDROID_STAGING_OIDC_AUTHORITY: ${{ inputs.oidc_authority }}
        run: ./gradlew assembleDevStaging

      - name: Upload staging APK
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: staging-apk
          path: android/app/build/outputs/apk/dev/staging/app-dev-staging.apk
          retention-days: 7
