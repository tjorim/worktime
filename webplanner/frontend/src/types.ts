export type Task = {
  id: string;
  text: string;
  tag: string;
  start: string;
  stop: string;
};

export type Template = {
  id: number;
  text: string;
  tag: string;
  start: string;
  stop: string;
};
